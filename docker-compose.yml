version: '3.8'
services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    image: wandersort:latest
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
    restart: unless-stopped
